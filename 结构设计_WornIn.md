# Worn-In「旧衣·新香」交互版 · 结构设计文档（v0.1 草案）

> 依据：① 你在分段编辑器里调好的四段构图（`wornin_project.json`）② 旧「衣橱变变变 / 魔镜调香」的 AI 产出与文案模板 ③ 两轮问题确认。
> 本文档写完后经你确认/修正，再开始大改代码。

---

## 0. 你已确认的决策（来自两轮提问）

| 项 | 结论 |
|---|---|
| 流程顺序 | 颤柜→烟雾①→镜颤点击→拍照上传→AI→丝带②(镜消失)→颤柜→衣柜打开③:三毛球染三主色+飘句 |
| 飘几句 | **完整 8 行都飘**（1开头 + 5中间 + 2收尾） |
| 文案生成 | **GPT 给属性 → 本地用衣橱变变变原版模板引擎拼文案**（文字风格 100% 沿用） |
| GPT 返回 | 全套照衣橱变变变：款式/颜色(3+hex)/印花/材质/领口/风格/变种 |
| 配方 | GPT 只选「风格+变种」，前/中/后调从原 **18 配方变种表** 取 |
| 染球颜色 | 用 GPT 返回的 3 个主色 hex 染三毛球白色/浅色部分（①左→色1 ②中→色2 ③右→色3） |
| 英文点缀 | 保留原版（如 “you really should try”） |
| 开口/收尾语气 | 保留（“哦，来客人了。”等） |
| 香料上球后 | 前/中/后调**常驻**在三个毛球上，底部给「再试一次」 |
| 降级按钮 | 加「示例结果」调试按钮（不联网也能看全流程） |
| 构图 | **严格用编辑器调好的四段构图**（读 `wornin_project.json`） |
| 上传方式 | **调用摄像头拍照**（iPad 竖屏，同魔镜调香人形虚线+倒计时；桌面无摄像头→文件选择兜底） |
| AI 失败 | 弹提示可重试 |
| 节奏 | 自动按 GIF 真实时长编排 |
| 音效 | 预留音效钩子，**真实音频文件你稍后提供**（先静音/占位） |

---

## 1. 全流程时间轴（最终确认版）

```
Phase 0  衣柜轻颤一次（≈0.6s，轻颤动效）← 直接从这开始，不播PPT开场(A)
Phase 1  烟雾飘（编辑器段1构图：柜+镜+烟雾GIF，按真实时长自动播完）
         └ 烟雾结束后：镜子不停颤抖 + 轻提示（等待点击）
Phase 1.5 点击镜子 → 拍照（iPad摄像头/桌面文件兑底）→ 拍完照片立刻显示在镜内(C)
         └ 同时后台开始 GPT 视觉分析（贯穿段2/段3）
Phase 2  丝带飘（段2构图）播放；期间 镜子(带照片) 从右上角→左下角 平移+淡出消失
Phase 3  衣柜再轻颤一次 → 衣柜打开（段3：柜子打开GIF，独立的一段，和毛球无关）
Phase 4  段4 放大场景：三毛球→液体(GIF 530帧 完整播，D) 三副本染成3主色
         └ 期间完整 8 行文案在画面中上区等间隔依次横飘
Phase 5  播完：前调/中调/后调 常驻显示在 ①②③ 三球上 + 底部「再试一次」
```

> 关键澄清(B/D)：段3「衣柜打开」与段4「三毛球→液体」是两段独立的动画。
> 三毛球染液只发生在**段4的放大背景+放大柜子**上，与段3互不干预。
> 段3开门播完 → 切段4放大场景 → 毛球开始制作过程。

---

## 2. 前端代码结构规划

放在 `C:\Users\janic\Downloads\编程project\Worn-In小网站\`：

```
play.html           交互主播放页（竖屏 iPad 适配）
play.css            布局/动画/字效样式
play.js             状态机 + 各 Phase 编排 + 计时
lib/
  compose.js        从 wornin_project.json 读取四段构图，绘制静态场景（背景+柜+镜等）
  gifs.js           GIF 帧播放器（读 assets/gifs/<key>/meta.json + frames/，含裁切/速度/染色）
  tint.js           染色：把 GIF 帧白色/浅色像素替换为主色（pre-render 缓存）
  camera.js         摄像头拍照（魔镜调香同款人形虚线+倒计时；无摄像头→文件兜底）
  ai.js             GPT 视觉调用 + prompt + 字段校验 + 示例数据
  copy.js           （从衣橱变变变移植）SPICE_EN/SPICE_IMG/SPICE_COLOR/COLOR_IMG/TEMPLATES/
                    RECIPES/pickVariant/genCopy —— 生成 8 行文案与配方
  sfx.js            音效钩子（WebAudio 占位，接口固定，真实音频后置替换）
wornin_project.json 你调好的四段构图（来源）
assets/gifs/…       已有 GIF 帧 + meta.json（复用）
assets/materials/…  已有素材（复用）
```

（现有 `index.html / anim.html` 为旧 PPT 定格放映器，保留不动；新交互页另起。）

---

## 3. AI 集成（GPT-4o-mini 视觉）

- 请求：`POST /v1/chat/completions`，model `gpt-4o-mini`，消息含 base64 衣物图 + 中文指令。
- Key：**不写进代码/不 push**。打开页面上方小设置（或首次进入）输入 key，仅存 `localStorage`（同魔镜调香 ⚙ 面板做法）。
- GPT 输出（JSON）：
```json
{
  "gar": "连衣裙", "fabric": "丝绸/缎面", "pattern": "豹纹/动物纹", "collar": "V领",
  "colors": [{"name": "藏青", "hex": "#1e2a4a"}, {"name": "白色", "hex": "#f5f2ec"}, {"name": "米色", "hex": "#d8cbb8"}],
  "style": "野性", "variant": "暗夜", "attrs": {...}
}
```
- 校验失败→重试按钮。附「示例结果」按钮直接注入一套内置 JSON 跑全流程。
- 拿到属性后：`pickVariant(style, attrs)` 选 18 配方变种 → `recipe{top,mid,base}`；`colors`(3) 用于染球；本地 `genCopy(属性)` 出 8 行。
- 视觉/文字不走代理即可用（已验证直连 200）。

---

## 4. 拍照（点镜子后）

- iPad：`getUserMedia({video:{facingMode:'user'}})` 全屏竖拍，人形虚线轮廓（头+上身），3·2·1 倒计时快门（复用魔镜调香样式与交互）。
- 桌面/无摄像头：自动降级为「选择文件」。
- 拍完即送 AI（可先出确认缩略图 + 🆗/重拍，同魔镜）。
- 照片同时用于：镜内显示（Phase1 末/2 中）与后续海报等（如日后要）。

---

## 5. 染色算法（tint.js）

- 输入：`assets/gifs/yarn_liquid/frames/fNNNN.png` 每帧，逐像素：`(r,g,b)` 高亮区 → 保留原透明度 alpha，将「白色/浅色」(亮度高 & 饱和度低) 像素染成目标主色，保留球面明暗（用原亮度缩放主色），非浅色像素保持原样。
- 每个副本一个目标色，按图层把整段帧 pre-render 到离屏 canvas 缓存（530 帧×3 色，第一次渲染可能稍慢→先按需缓存 + loading）。
- 8 行文案飘在画面中上（CSS 动画，非画布内，便于换行/字体）。

---

## 6. 音效（sfx.js 占位钩子）

- 固定接口：`SFX.tremble() / mirrorTremble() / click() / shutter() / sentence() / open() / complete()`。
- 现在为静音占位；你日后给真实音频文件（放 `assets/sfx/`，命名 `tremble.mp3` 等）即自动替换，不改逻辑。

---

## 7. 已确认开放点（用户已拍板）

- **A**：直接从衣柜轻颤开始，**不保留** 63 帧 PPT 定格开场。
- **B**：三毛球染液+飘句的“制作过程”发生在**段4放大背景+放大柜子**。
- **C**：拍完照**立刻**显示进镜子；AI 分析在**段2丝带 + 段3衣柜打开**期间后台进行。
- **D**：段4 毛球 530 帧 GIF **完整段播放**（段3开门和段4毛球是两段独立动画）。

---

## 8. 交付流程

1. 我实现 `play.html` 全流程 → 2. 本地 8904 预览，你验收 → 3. 你点头后推 GitHub Pages。
